import './{{ pascal.layoutName }}.scoped.css';
import { Outlet } from 'react-router-dom';

/*
 * import Navbar from "./components/Navbar/Navbar";
 * import Footer from "./components/Footer/Footer";
 */

function {{ pascal.layoutName }}(): JSX.Element {
  return (
    <div {% raw %}style={{ height: '100%' }}{% endraw %}>
      {/* <NavBar /> */}
      <div className="page-content">
        <Outlet />
      </div>
      {/* <Footer /> */}
    </div>
  );
}

export default {{ pascal.layoutName }};